export interface RoutedInClaims {
  claimId: number;
  claimType: string;
  receiptDate: Date;
  claimAge: string;
  queueName: string;
  auditorName: string;
  processedDate: Date;
  routeReason: string;
  claimStatus: string;
}
