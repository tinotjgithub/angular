export interface MyClaims {
  claimId: number;
  receivedDate: string;
  selectedReason: any;
  age: string;
  status: string;
  queueName: string;
  pendReason: string;
  firstPendDate: Date;
  recentPendDate: Date;
  comments: string;
}
